{
  "name": "sveltekit-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'SvelteKit demo package built'",
    "test": "vitest run",
    "clean": "rm -rf dist .svelte-kit *.tsbuildinfo"
  },
  "dependencies": {
    "@critical-path/client": "workspace:*",
    "@critical-path/core": "workspace:*",
    "@critical-path/server": "workspace:*",
    "@critical-path/svelte": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "svelte": "^5.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
