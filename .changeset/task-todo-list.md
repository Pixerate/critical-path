---
"@critical-path/core": minor
"@critical-path/svelte": minor
---

Add first-class optional todo list (checklist) support to Task model and TaskEntity with helper methods (addTodo, toggleTodo, removeTodo) and updateTask helper in @critical-path/svelte.
