---
'@critical-path/client': minor
---

Add subtask and checklist CLI commands (`critical-path subtask`, `critical-path checklist`) and CriticalPathClient helpers (`createSubtask`, `addTodo`, `toggleTodo`).
